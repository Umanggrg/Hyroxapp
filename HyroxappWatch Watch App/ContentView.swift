// Obsolete — Xcode's "New watchOS App" template generated this file, but
// the app entry point (`HyroxappWatchApp`) now mounts `WatchRaceView`
// directly, so `ContentView` is no longer referenced anywhere.
//
// Safe to delete this file at your convenience: right-click in the Xcode
// sidebar → Delete → "Move to Trash". Left in place for now because our
// file tooling can't delete inside Xcode-owned target folders — pure
// housekeeping, no functional impact either way.
